@props(['title', 'description' => null])
<div {{ $attributes->class(['mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between']) }}><div><h2 class="text-2xl font-bold tracking-tight text-gray-900 dark:text-white">{{ $title }}</h2>@if($description)<p class="mt-1 text-sm text-gray-500 dark:text-gray-400">{{ $description }}</p>@endif</div>@isset($actions)<div class="flex flex-wrap items-center gap-2">{{ $actions }}</div>@endisset</div>
